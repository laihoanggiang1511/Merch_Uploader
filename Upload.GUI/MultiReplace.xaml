<Window x:Class="Upload.GUI.MultiReplace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Upload.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <StackPanel Orientation="Vertical">
        <DataGrid ItemsSource="{Binding ListShirts}">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PNGPath}" CanUserResize="True" Header="PNG Path" IsReadOnly="True" Width="150"/>
                <DataGridTextColumn Binding="{Binding Descriptions}" CanUserResize="True" Header="Descriptions" Width="300"/>
                <DataGridTextColumn Binding="{Binding Replace}" Header="Replace" Width="150"/>
                <DataGridTextColumn Binding="{Binding With}" Header="With" Width="150"/>
            </DataGrid.Columns>-->
        </DataGrid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Replace All" Margin="5" Width="120"/>
            <Button Content="Get File Name" Margin="5" Width="120"/>
            <Button Content="Close" Margin="5" Width="120"/>

        </StackPanel>
    </StackPanel>
</Window>
