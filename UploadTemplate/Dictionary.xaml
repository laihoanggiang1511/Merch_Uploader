<Window x:Class="UploadTemplate.Dictionary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UploadTemplate"
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <DataGrid Name="dictionary" VerticalAlignment="Top" Margin="5" HorizontalAlignment="Center" 
                  CanUserAddRows="True" CanUserDeleteRows="True" AlternatingRowBackground="AliceBlue">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Replace" Width="150"/>
                <DataGridTextColumn Header="With" Width="300"/>

            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Width="100" Margin="5" Content="Add"  BorderThickness="0"/>
            <Button Width="100" Margin="5" Content="Remove" Background="Crimson" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
