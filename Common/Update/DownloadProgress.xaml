<Window x:Class="Common.Update.DownloadProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Common.Update"
             mc:Ignorable="d" WindowStyle="ToolWindow"  Title="UPDATE"
             Width="500" Height="100" WindowStartupLocation="CenterScreen">
    <Grid Height="25" Margin="10">
        <ProgressBar Name="DownloadProgressBar" Height="25" Minimum="0" Maximum="100"/>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="DemiBold">
            <Run> Downloading </Run>
            <Run Text="{Binding ElementName=DownloadProgressBar,Path=Value}"></Run>
            <Run Text="%"></Run>
        </TextBlock>
    </Grid>
</Window>
